extends base
include elements/post

block content
    table.itemlist(border="0" cellpadding="0" cellspacing="0")
        - var count = 0
        each post in posts
            - count += 1
            - post.count = count
            + post(post, url)
            tr.spacer(style="height:12px")